PLACE YOUR LOGO HERE
====================

Save the approved logo file in this folder with the EXACT name:

    logo.png

Full path:
    assets/img/logo.png

The whole site (every page header, footer, the language splash page, and the
404 page) already references this file. Until it exists, each location shows a
clean text wordmark fallback automatically — nothing looks broken. As soon as
logo.png is added, the image appears everywhere with no further edits.

Notes:
- The logo is displayed on a black plate, so the white-on-black PNG sits
  cleanly on both the white header and the dark-green footer.
- For the sharpest header fit, a tightly-cropped version (minimal black margin
  around the artwork) looks best. The full file works too.
- Optional extras you can add later for polish:
    favicon.png        (browser tab icon, ~512x512)
    og-image.png       (social share preview, 1200x630)
  If you add these, tell me and I'll wire them into every page's <head>.
